import React from 'react';
import { Link } from 'react-router';

const Plants = () => {
    return (
        <div>
            <Link to="/login">Login</Link>
        </div>
    );
};

export default Plants;